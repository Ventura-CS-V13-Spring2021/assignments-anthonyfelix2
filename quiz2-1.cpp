#include <iostream>
#include <iomanip>
#include <fstream>
#include <cstdlib>
#include <ctime>
using namespace std;
// tried doing without creating a file but i cannot figure it out
int main()
{
int rdnum0, rdnum1, rdnum2, rdnum3, rdnum4, rdnumN, rdnum;
int N = 5;

srand(time(0));
	for(int i; i<N; i++)
	{
		rdnum = random() % 100;
    rdnum0 = rdnum
    rdnum1 = rdnum
  }
  
}